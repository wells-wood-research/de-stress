name: Big Structure Tests

on:
  push:
    branches:
      - master
    paths:
      - '.github/workflows/big-structure-run-tests.yml'
      - 'big-structure/**'
  pull_request:
    paths:
      - '.github/workflows/big-structure-run-tests.yml'
      - 'big-structure/**'

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build `big-structure`
      run: |
        cd big-structure/
        cp .env-testing .env
        docker build -t big-structure .
    - name: Clone de-stress dependencies
      uses: actions/checkout@v1
      with:
        repository: wells-wood-research/de-stress_dependencies.git
        token: ${{ secrets.DEPENDENCIES_ACCESS_TOKEN }}
        ref: master
        path: de-stress_dependencies/
    - name: Running tests.
      run: |
        sudo chmod -R 755 big-structure/dependencies_for_isambard/
        cp -r /home/runner/work/de-stress/de-stress_dependencies/ big-structure/dependencies_for_isambard/de-stress_dependencies/
        cd big-structure/dependencies_for_isambard/
        ls -l 
        cd ../
        docker run --rm big-structure poetry run pytest
      

